import { PlaceholderPage } from "@/lib/ui/placeholder-page";

export default function LoginPage() {
  return (
    <PlaceholderPage
      eyebrow="Auth module"
      title="Login pendent d'implementar"
      description="La ruta existeix per validar l'estructura base del mòdul d'autenticació. La lògica de login arribarà en les tasks següents."
    />
  );
}
